@using KarYemek.Models.FoodViewModels
@model GetFoodViewModel

@{
    ViewData["Title"] = "Yiyecek Detayları";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="text-center my-4">Yiyecek Detayları</h1>

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header bg-info text-white">
                <h4>@Model.Name</h4>
            </div>
            <div class="card-body">
                <!-- Food Image Display -->
                <div class="text-center mb-3">
                    @if (!string.IsNullOrEmpty(Model.ImageURL))
                    {
                        <img src="@Model.ImageURL" alt="Yemek Fotoğrafı" class="img-fluid img-thumbnail" style="max-height: 300px; max-width: 100%;" />
                    }
                    else
                    {
                        <p class="text-muted">Yemek fotoğrafı mevcut değil.</p>
                    }
                </div>

                <dl class="row">
                    <dt class="col-sm-3">Açıklama</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Description)</dd>

                    <dt class="col-sm-3">Kalori</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Calorie)</dd>
                </dl>
            </div>

            <div class="card-footer text-right">
                <!-- Silme Onayı Butonu -->
                <button class="btn btn-danger" data-toggle="modal" data-target="#deleteConfirmationModal">Sil</button>
                <a asp-action="Index" class="btn btn-secondary">Listeye Geri Dön</a>
            </div>
        </div>
    </div>
</div>

<!-- Silme Onayı Modalı -->
<div class="modal fade" id="deleteConfirmationModal" tabindex="-1" role="dialog" aria-labelledby="deleteConfirmationLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteConfirmationLabel">Silme Onayı</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Kapat">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p class="text-danger">Bu yiyecek maddesini kalıcı olarak silmek istediğinizden emin misiniz? Bu işlem geri alınamaz.</p>

                <!-- Fotoğraf Önizlemesi -->
                <div class="text-center">
                    <img src="@Model.ImageURL" alt="Yemek Fotoğrafı" class="img-fluid img-thumbnail" style="max-height: 200px; max-width: 100%;" />
                </div>
            </div>
            <div class="modal-footer">
                <form asp-action="Delete" method="post">
                    <input type="hidden" asp-for="Id" />
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">İptal</button>
                    <button type="submit" class="btn btn-danger">Sil</button>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- Bootstrap ve jQuery için Gerekli JavaScript -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>
